/**
 * The Dashboard.
 *
 * It holds nothing yet. There is no item model behind it, so anything drawn here — counts,
 * a summary, the four actions T-01 sketches — would describe a product the vault cannot hold
 * and offer controls that write nowhere. The route stays because it is where first-run setup
 * hands over: the gate above it is what decides between the wizard and this page.
 */
export default function DashboardPage() {
  return null;
}
