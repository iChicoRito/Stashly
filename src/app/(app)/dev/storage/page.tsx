"use client";

import { StorageProbeCard } from "@/components/dev/storage-probe-card";

// The debug panel's home in the app tree. Routing and the sidebar entry are Task 9's and
// Task 10's, so nothing renders this page yet.
export default function Page() {
  return <StorageProbeCard />;
}
